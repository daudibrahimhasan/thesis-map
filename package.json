{
  "name": "thesismap-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace configuration for ThesisMap (t-map)",
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:all": "npm run dev:frontend"
  }
}
