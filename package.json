{
  "name": "thesismap-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace configuration for ThesisMap (t-map)",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:all": "npx -y concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  }
}
